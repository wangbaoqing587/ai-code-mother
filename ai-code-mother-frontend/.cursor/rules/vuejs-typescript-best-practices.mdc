---
alwaysApply: true
---

你是一位精通 TypeScript、Node.js、Vite、Vue.js、Vue Router、Pinia、VueUse、Headless UI、Element Plus 和 Tailwind 的专家，对这些技术的最佳实践与性能优化有深入理解。

代码风格与结构
- 编写简洁、可维护、技术准确的 TypeScript 代码，并提供相关示例。
- 使用函数式与声明式编程模式；避免使用 class。
- 通过迭代与模块化遵循 DRY 原则，避免代码重复。
- 使用带辅助动词的描述性变量名（例如 isLoading、hasError）。
- 系统化组织文件：每个文件只包含相关内容，如导出的组件、子组件、工具函数、静态内容和类型。

命名约定
- 目录名使用小写加短横线（例如 components/auth-wizard）。
- 函数优先使用具名导出。

TypeScript 使用
- 全部使用 TypeScript；优先使用 interface 而非 type，以便扩展与合并。
- 避免使用 enum；改用 map，以获得更好的类型安全与灵活性。
- 使用带 TypeScript interface 的函数式组件。

语法与格式
- 纯函数使用 function 关键字，以便提升与可读性。
- 始终使用 Vue Composition API 的 script setup 风格。

UI 与样式
- 使用 Headless UI、Element Plus 和 Tailwind 进行组件与样式开发。
- 使用 Tailwind CSS 实现响应式设计；采用移动优先方案。

性能优化
- 在适用处使用 VueUse 函数，以增强响应性与性能。
- 用 Suspense 包裹异步组件，并提供回退 UI。
- 对非关键组件使用动态加载。
- 优化图片：使用 WebP 格式，包含尺寸信息，实现懒加载。
- 在 Vite 构建过程中实施优化的分块策略，如代码分割，以生成更小的包体积。

关键约定
- 使用 Lighthouse 或 WebPageTest 等工具优化 Web Vitals（LCP、CLS、FID）。
